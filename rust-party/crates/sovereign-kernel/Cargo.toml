[package]
name = "sovereign-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sovereign-types = { path = "../sovereign-types" }
thiserror = "1.0"
